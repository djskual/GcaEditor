<Window x:Class="GcaEditor.Views.ChooseCarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose car"
        Height="235"
        Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppWindowBackgroundBrush}"
        Foreground="{DynamicResource AppTextBrush}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top row: custom mode + side -->
        <Grid Grid.Row="0" Margin="0,3,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CheckBox x:Name="CustomCheck"
                      Grid.Column="0"
                      Style="{StaticResource LabeledCheckBoxStyle}"
                      Content="Custom (manual mode)"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center"
                           Text="Drive Side"
                           Margin="0,0,8,0"
                           Foreground="{DynamicResource AppMutedTextBrush}"/>

                <RadioButton x:Name="LhdRadio"
                             Content="LHD"
                             IsChecked="True"
                             Margin="0,0,12,0"/>

                <RadioButton x:Name="RhdRadio"
                             Content="RHD"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Margin="0,0,0,4" Text="Car" />
        <ComboBox x:Name="CarCombo" Grid.Row="2" MinWidth="380" Height="28"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock VerticalAlignment="Center" Text="MIB" Margin="0,0,8,0" />
            <ComboBox x:Name="MibCombo" Width="120" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,20,8,0" Click="Cancel_Click" />
            <Button Content="OK" Width="90" Margin="0,20,0,0" Click="Ok_Click" />
        </StackPanel>

    </Grid>
</Window>
    