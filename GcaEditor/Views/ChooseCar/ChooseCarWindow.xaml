<Window x:Class="GcaEditor.Views.ChooseCarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose car"
        Height="260"
        Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="CustomCheck" Grid.Row="0" Content="Custom (manual mode)" />

        <TextBlock Grid.Row="1" Margin="0,10,0,4" Text="Car" />
        <ComboBox x:Name="CarCombo" Grid.Row="2" MinWidth="380" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock VerticalAlignment="Center" Text="MIB" Margin="0,0,8,0" />
            <ComboBox x:Name="MibCombo" Width="120" />

            <TextBlock VerticalAlignment="Center" Text="Side" Margin="18,0,8,0" />
            <RadioButton x:Name="LhdRadio" Content="LHD" IsChecked="True" Margin="0,0,12,0" />
            <RadioButton x:Name="RhdRadio" Content="RHD" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="OK" Width="90" Click="Ok_Click" />
        </StackPanel>

    </Grid>
</Window>
